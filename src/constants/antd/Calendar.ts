// Enum สำหรับ AntD Calendar / DatePicker

// ประเภทของ Picker
export enum EnumAntdPickerType {
  DATE = "date",
  WEEK = "week",
  MONTH = "month",
  QUARTER = "quarter",
  YEAR = "year",
  RANGE = "range",
}

// ขนาดของ Picker
export enum EnumAntdPickerSize {
  SMALL = "small",
  MIDDLE = "middle",
  LARGE = "large",
}

// รูปแบบการแสดงผลของวันที่
export enum EnumAntdPickerFormat {
  YYYY_MM_DD = "YYYY-MM-DD",
  DD_MM_YYYY = "DD-MM-YYYY",
  MM_DD_YYYY = "MM-DD-YYYY",
  YYYY_MM = "YYYY-MM",
  YYYY = "YYYY",
}

// Mode ของ Calendar / DatePicker
export enum EnumAntdPickerMode {
  DATE = "date",
  MONTH = "month",
  YEAR = "year",
  DECADE = "decade",
}
